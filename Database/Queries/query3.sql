select Email,Fullname,NumReports from num_engager_reports
where NumReports>2

/* In the example given in report, the query returns email ,fullname, NumReports
for engagers that have  more than 10 reports. Due to lack of data,
the query returns email ,fullname,NumReports for engagers that have  more than 2 Reports.*/
